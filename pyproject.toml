[project]
name = "dunequeryrepo"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "dune-client",
    "pandas",
    "python-dotenv",
    "pyyaml",
]

[tool.uv]
exclude-newer = "3 days"
no-build = true
